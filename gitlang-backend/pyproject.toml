[project]
name = "gitlang-backend"
version = "0.1.0"
description = "The backend for the vocabulary repository app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.3",
    "fastapi>=0.124.2",
    "uvicorn>=0.38.0",
]
